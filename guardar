transform transition-transform duration-300 ${activeCard === service.id ? "bg-blue-200" : ""
                          }`} // Removi o /50 para garantir que o fundo azul fique visível
                        onClick={() => handleCardClick(service.id)}